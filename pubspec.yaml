name: context_budget
description: >-
  Token-accurate context budgeting for repositories, plus a live renderer for
  streaming structured model output.
version: 0.1.0
publish_to: none
repository: https://github.com/Yusufihsangorgel/context_budget

environment:
  sdk: ^3.10.0

dependencies:
  args: ^2.5.0
  hf_tokenizers: ^0.1.0
  stream_struct: ^0.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
